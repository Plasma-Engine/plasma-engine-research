[pytest]
addopts = -ra
testpaths = tests
pythonpath = app
markers =
    unit: Fast-running tests that exercise individual functions.
    integration: Tests that cover component interactions or IO boundaries.
    performance: Benchmarks and throughput checks that may take longer to run.
    slow: Explicitly marked slow tests; skip in quick feedback loops when needed.

